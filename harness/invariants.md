# MCP Visor — harness invariants

Each invariant maps to automated checks in `check.sh` (via `go test`) or manual demo steps.

| ID | Invariant | Verification |
|----|-----------|--------------|
| H1 | Default deny for unknown tools | `TestUnknownToolDenied` — `tests/integration/extended_test.go` |
| H2 | Sensitive paths (e.g. `.env`) denied | `TestSensitiveFileAccessDenied` — `tests/integration/extended_test.go` |
| H3 | Read → send chain denied | `TestChainDetectionFileReadThenHTTPPost`, `TestChainDetectionDatabaseQueryThenSlack` — `tests/integration/chain_detection_test.go` |
| H4 | Chain events audited | `TestChainDetectionAuditEvent` — `tests/integration/chain_detection_test.go` |
| H5 | Audit log redacts secrets | `TestAuditLogRedaction` — `tests/integration/extended_test.go` |
| H6 | Proxy handshake + tools/call path works | `TestProxyIntegrationHandshake`, `TestProxyIntegrationToolsCall` — `tests/integration/proxy_integration_test.go` |
| H7 | Policy lint catches invalid YAML/rules | `go test ./internal/policy/...` (linter package) |
| H8 | No LLM in policy engine | Code review + `internal/policy/engine.go` has no LLM client imports |
| H9 | An authorized sensitive-source request taints the session | `TestSessionTaintEgressDenyBlocksSinkAfterSensitiveSource` — `internal/proxy/session_taint_test.go` |
| H10 | A tainted egress request is denied with audit metadata before relay | Same test + `TestSessionTaintEgressAllowsSinkBeforeTaint` — `internal/proxy/session_taint_test.go` |
| H11 | Audit hash linkage for healthy writes, including recovery of prev_hash/chain_index when reopening an existing JSONL file; incomplete tails and corrupt last records fail closed | `TestAuditLogHashChain`, `TestNewLoggerRecoversHashChainAcrossRestart`, `TestNewLoggerRejectsIncompleteTrailingLine`, `TestNewLoggerRejectsMalformedLastRecord` — `internal/audit/logger_test.go` |
| H12 | Notification-form `tools/call`, including typed duplicate-key parser differentials, JSON-RPC batching, and the post-initialize handshake slot, is blocked on stdio and remote; non-tools notifications and batches still forward | `TestClassifyClientEnvelopeDeniesTypedDuplicateNotificationToolsCall`, `TestClassifyClientEnvelopeDeniesDuplicateMethodWhenToolsCallAppearsFirst`, `TestClassifyClientEnvelopeDeniesDuplicateMethodWhenToolsCallAppearsLast`, `TestClassifyClientEnvelopeDeniesDuplicateMethodWhenToolsCallAppearsLastWithID`, `TestClassifyClientEnvelopeForwardsNonToolsBatch`, `TestClassifyClientEnvelopeDeniesBatchContainingToolsCallNotification`, `TestClassifyClientEnvelopeDeniesBatchContainingToolsCallRequest`, `TestClassifyClientEnvelopeDeniesBatchWithLeadingWhitespace`, `TestInterceptDeniesNotificationToolsCallStdio`, `TestInterceptDeniesNotificationToolsCallRemoteParity`, `TestInterceptForwardsInitializedNotificationStdio`, `TestInterceptForwardsInitializedNotificationRemote`, `TestInterceptDeniesBatchContainingToolsCall`, `TestInterceptDeniesHandshakeNotificationToolsCall`, `TestInterceptForwardsHandshakeInitialized`, `TestProxyIntegrationNotificationToolsCallNotRelayed` |
| H13 | A denied handshake terminates a waiting stdio server process without hanging cleanup | `TestStopServerProcessTerminatesWaitingChild` — `internal/proxy/tools_call_envelope_test.go` |
| H14 | A plain allowed `tools/call` emits a standalone JSONL audit event (not only in-memory session history) | `TestAllowedToolsCallEmitsStandaloneJSONLAuditEvent` — `internal/proxy/tools_call_audit_test.go` |
| H15 | Input redaction never emits a premature allow audit; only the terminal decision event is written, with redaction noted | `TestRedactionDoesNotEmitPrematureAllowAudit`, `TestDeniedAfterRedactionEmitsOnlyDenyAudit` — `internal/proxy/tools_call_audit_terminal_test.go` |
| H16 | Incomplete TLS client cert/key pairs fail closed; HTTP SSE read cannot block concurrent message writes | `TestTLSConfigRejectsIncompleteClientKeyPair`, `TestHTTPTransportAllowsConcurrentReadAndWrite` — `internal/transport/transport_test.go` |
| H17 | Successful policy hot reload atomically refreshes engine rules, redactor, audit patterns, and approval timeout; invalid reloads keep prior runtime surfaces | `TestHotReloadAtomicallyRefreshesRedactorAuditAndApproval`, `TestHotReloadInvalidPolicyKeepsRuntimeSurfaces` — `internal/proxy/hot_reload_test.go`; watcher keep-current tests in `internal/policy` |
| H18 | Built-in SIEM JSON export is a reduced envelope without hash-chain fields or arguments (not a substitute for JSONL retention) | `TestJSONFormat` — `internal/siem/siem_test.go` |
| H19 | No valid `tools/call` is relayed unless its final allow record has been durably committed to the trusted audit sink opened by Visor (append complete + explicit file sync before relay; any marshal/write/short-write/sync/closed/poison/non-durable failure poisons the sink, denies with zero relay, and never advances chain/taints/allowed/approved state) | `TestNewLoggerRejectsFinalSymlink`, `TestNewLoggerSyncsParentUnconditionally`, `TestCommitAuthorizationWritesThenSyncsBeforeAdvancing`, `TestCommitAuthorizationShortWritePoisonsWithoutAdvancing`, `TestCommitAuthorizationSyncFailurePoisonsWithoutAdvancing`, `TestCommitAuthorizationRejectsNonAllowAndNonDurableSink`, `TestLogShortWriteReturnsErrorAndPoisons` — `internal/audit/logger_test.go`, `internal/audit/logger_commit_internal_test.go`; `TestAuthorizationCommitFailureDeniesWithoutRelay`, `TestAuthorizationCommitPrecedesRelay`, `TestAuthorizationCommitFailureDeniesRemoteWithoutRelay`, `TestAuthorizationCommitFailureAfterApprovalDeniesWithoutRelay`, `TestAuthorizationCommitRequiresConfiguredDurableSink` — `internal/proxy/tools_call_authorization_commit_test.go` |
| H20 | A configured stdio server identity is resolved independently of MCP metadata: the locally resolved launcher executable and every literal argv value are always content/literal-bound under a versioned, injectively framed SHA-256 serialization (`stdio_invocation_sha256_v1`; each component carries a domain tag, a fixed-width big-endian index, and an explicit byte length, so different invocation structures — executable+argv vs an executable embedding the same bytes, `[a b]` vs `[a\x00b]`, reordered args, same bytes as executable content vs argument, identical payloads at different declared positions — never serialize to the same hash input), while ONLY the policy-declared entry argument positions (`attestation.entry_arg_positions`, zero-based indexes into `ServerArgs` excluding the executable) bind the resolved local regular-file content; undeclared file-valued args (logs, databases, datasets, output paths) are never opened or hashed, so mutable runtime data cannot change the digest; recognized canonical dynamic registry runners (`npx`, `uvx`, `bunx`, `pnpx`, `pnx`, `npm exec`, `npm x`, `yarn dlx`, `pnpm dlx`, `bun x`, `uv tool run`) are unpinnable and fail closed when attestation is configured; only exact canonical executable names and exact leading subcommand tuples are recognized, so ordinary non-registry subcommands (`npm run`, `npm install`, `npm ci`, `yarn add`, `pnpm add`, `pnpm exec`, `bun run`, `bun add`, `uv run`, `uv tool install`) remain resolvable; loader rejects negative/duplicate positions and resolver rejects out-of-range/missing/non-regular/read-failure declared entries; the policy loader accepts exactly one attestation kind (`stdio_invocation_sha256_v1`) and rejects unknown or legacy scheme names; a logical server with no attestation performs zero identity-resolution work in both `New` and `NewWithTracing`; identity is resolved exactly once at proxy construction and is immutable for the proxy/stdio-child lifecycle, including its resolution shape (attestation kind plus normalized entry_arg_positions); hot reload never re-hashes launcher or payload paths; a pin introduced after an unattested start, a changed resolution shape, or a same-shape digest replacement compares against the cached startup identity and fails closed as unresolved/restart-required until restart; removal may restore the explicitly requested unattested legacy path; an originally unattested process remains unresolved on later reintroduction; a startup resolution failure stays unresolved and is not retried on reload; approval terminal allow/deny events carry one complete policy plus identity snapshot captured before the runtime barrier was released; the identity is checked before argument policy or relay, and mismatch/unresolved denies with identity-bound audit evidence while a matching local identity passes. Boundary (decision A 2026-08-12): attestation is entry-artifact attestation, not dependency-closure attestation — transitive interpreted dependencies (imported modules, installed packages) are NOT pinned | `TestServerIdentityMismatchDeniesBeforeArgumentPolicy`, `TestServerIdentityMatchAllowsToolCall`, `TestServerIdentityUnpinnableRegistryLauncherFailsClosed`, `TestServerIdentityUnpinnableRegistryLauncherRejected`, `TestServerIdentityNpmRunNotRejected`, `TestServerIdentityDocumentedRegistryRunnersRejected`, `TestServerIdentityDocumentedRegistryRunnersFailClosed`, `TestServerIdentityNonRegistryPackageManagerSubcommandsNotRejected`, `TestServerIdentityApprovalSigningFailureUsesPolicySnapshot`, `TestServerIdentityUndeclaredRuntimeFileDoesNotChangeDigest`, `TestServerIdentityUndeclaredRuntimeFileDoesNotChangeDigestAtProxy`, `TestServerIdentityDeclaredEntryMutationChangesDigest`, `TestServerIdentityDeclaredEntryValidation`, `TestServerIdentityDeclaredEntryOrderIndependence`, `TestServerIdentityNoAttestationPerformsZeroResolverWork`, `TestServerIdentityReloadDoesNotReResolveRunningProcess`, `TestServerIdentityPinIntroAfterLaunchFailsClosedRequiresRestart`, `TestServerIdentityChangedShapeCannotReuseOldDigest`, `TestServerIdentityShapeNormalizedOrderCompatibility`, `TestServerIdentityApprovalAllowUsesIdentitySnapshot`, `TestServerIdentityApprovalSigningFailureUsesPolicySnapshot`, `TestServerIdentityReloadResolverErrorFailsClosed`, `TestServerIdentityPolicyAcceptsDeclaredEntryPositions`, `TestServerIdentityPolicyRejectsInvalidEntryPositions`, `TestFramingCodexExecutableArgCollision`, `TestFramingArgumentNULNotBoundary`, `TestFramingArgumentOrder`, `TestFramingDomainSeparation`, `TestFramingEntryPayloadPosition`, `TestFramingPayloadMutationChangesDigest`, `TestFramingDeterministic`, `TestFramingFieldSelfDelimiting`, `TestServerIdentityPolicyAcceptsPinnedStdioSHA256`, `TestServerIdentityPolicyRejectsUnsupportedKind`, `TestConfusedDeputyDemo` |
| H21 | An `allow_recipient` rule is an exact mandated mailbox slot: missing, blank, or non-string recipient, an empty allowlist, and any non-exact mailbox (including same-domain and suffix spoofs) deny before relay; every present destination alias (`recipient`/`to`/`email`/`cc`/`bcc`) must independently be an allowlisted string, so a mandated mailbox in one key cannot cover an attacker mailbox in another; duplicate JSON keys in `tools/call` arguments fail closed so a last-wins decode cannot authorize first-wins relayed bytes; authorized invoice-path and mandated-recipient instantiations allow; repeating a denied recipient in the same session does not authorize it | `TestAuthorityNonEscalationInstantiationAllowed`, `TestAuthorityNonEscalationExpansionDenied`, `TestAuthorityNonEscalationEmptyAllowlistFailsClosed`, `TestAuthorityNonEscalationNonStringRecipientFailsClosed`, `TestAuthorityNonEscalationEveryPresentAliasIsChecked`, `TestAuthorityNonEscalationDuplicateArgumentKeysFailClosed`, `TestAuthorityNonEscalationHistoryDoesNotPromoteRecipient` — `internal/policy/authority_non_escalation_test.go`; `TestAuthorityNonEscalationProxyDeniesInjectedRecipientBeforeRelay`, `TestAuthorityNonEscalationProxyDeniesShadowAliasBeforeRelay`, `TestAuthorityNonEscalationProxyDeniesDuplicateArgumentKeysBeforeRelay`, `TestAuthorityNonEscalationProxyHistoryDoesNotPromoteRecipient` — `internal/proxy/authority_non_escalation_test.go` |

**Prompt-injection immunity** is architectural: decisions do not parse natural language from tool descriptions in an LLM. Regression: policy engine tests + integration deny paths; document scenarios in `examples/malicious-prompts/`.

**Policy-validation limitation:** loader rejects YAML/schema errors. `lint --strict` fails reported warnings, but `deny_command_pattern_composite` is recognized without enforcement and `--no-warnings` can neutralize strict warning failures. It is not yet a complete deployment gate.

## Adding an invariant

1. Add a row here with a test name or lint rule.
2. Add or extend integration test under `tests/integration/`.
3. Run `harness/check.sh` and attach evidence manifest.