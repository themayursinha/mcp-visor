{
  "task_id": "T-WORKFLOW-001-fixture-only",
  "invariant_ids": ["H1"],
  "security_sensitive": true,
  "security_problem": "Agents must not claim verification without contract, scope, executed commands, and optional review.",
  "required_behavior": "visor-workflow derives status from artifacts only; fake exits are impossible.",
  "failure_behavior": "Missing RED/target/harness or out-of-scope paths block higher status.",
  "allowed_paths": [
    "harness/tasks/examples/T-WORKFLOW-001-fixture-only.json",
    "harness/fixtures/workflow-smoke.txt"
  ],
  "approval_gated_paths": [],
  "max_attempts": 2,
  "required_commands": [
    {"name": "red_test", "expect": "fail"},
    {"name": "target_test", "expect": "pass"},
    {"name": "harness", "expect": "pass"}
  ]
}
